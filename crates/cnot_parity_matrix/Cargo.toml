[package]
name = "cnot_parity_matrix"
version = "0.0.1"
edition = "2024"

[dependencies]
bits = { path = "../bits" }
test_core = { path = "../core" }

[dev-dependencies]
rand = "0.10.0"
rand_chacha = "0.10.0"
